# implement a number of visualizations of the model
# requires both that a model.pickle and training database exists
import pickle
import cv2
import numpy as np 
from eigen_face_generator import getLabelledFaces
